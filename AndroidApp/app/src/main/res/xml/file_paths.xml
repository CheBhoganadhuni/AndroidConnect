<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Allows FileProvider to serve files from the app's cache dir (used for APK downloads) -->
    <cache-path name="apk_download" path="." />
</paths>
